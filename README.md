# EGFWD Reads Project

The EGFWD first project (Reads)


### Prerequisites
    1. nodejs
    2. npm or yarn


### Installation

``` npm install ``` or ``` yarn install ```


### Run

``` npm start ``` or ``` yarn start ```
